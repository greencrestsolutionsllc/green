# Greencrest design spec

> **Owned by Claude Design.** Build (Claude Code) reads this file and never edits it.

**Status: empty.** The Perspectives site (D-003–D-012) was cancelled by the owner on
2026-09-25 and its spec, brief and mockups were removed. They remain in git history
(last full version at commit `6d8f1cf`).

The next spec goes here. For the section layout, see `briefs/_TEMPLATE.md` and `/CLAUDE.md`.
